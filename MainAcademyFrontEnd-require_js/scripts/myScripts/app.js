define(['jquery', 'mymethods','coolmet','awemeth'],function ($, meth, coolmet, awemeth) {
    meth.logConsole("Finally!");
        meth.showAllert("lll");
    var hi = meth.hello;
        $("body").html(hi);
        awemeth.question;
        awemeth.confirm;
});